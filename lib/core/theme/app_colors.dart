import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Futuristic / Stylized Market Palette (Placeholder-ready)
  static const Color primary = Color(0xFF00FFCC); // Neon Green/Cyan
  static const Color secondary = Color(0xFFFF007F); // Neon Pink
  static const Color background = Color(0xFF0B0C10); // Deep Cyber Dark
  static const Color surface = Color(0xFF1F2833); // Dark Grey Surface

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFC5C6C7);

  static const Color accentGold = Color(0xFFFFD700); // For coins/shards
  static const Color errorRed = Color(0xFFFF4C4C);
}
